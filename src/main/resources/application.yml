spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cooking
    username: cooking
    password: cooking
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
server:
  port: 8080
app:
  seed:
    enabled: true